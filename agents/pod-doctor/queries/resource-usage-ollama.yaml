apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: resource-usage-ollama
spec:
  input: "Show CPU and memory usage for the ollama pod in the ollama-system namespace."
  target:
    name: pod-doctor
    type: agent
