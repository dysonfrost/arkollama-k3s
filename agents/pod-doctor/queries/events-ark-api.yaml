apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: events-ark-api
spec:
  input: "Show recent events for the ark-api pod in default namespace."
  target:
    name: pod-doctor
    type: agent
