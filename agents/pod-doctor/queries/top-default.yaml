apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: top-default
spec:
  input: "Show CPU and memory usage for all pods in the default namespace."
  target:
    name: pod-doctor
    type: agent
